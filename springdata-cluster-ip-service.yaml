apiVersion: v1
kind: Service
metadata:
  name: pspringdata-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: springdata
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
